<% provide(:title, "Employer Insurance Rates - History") %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @country.country %>
      </h1>
    </section>
    <section>
      <div class="instruction">
        <%= render "insurance_rates/instructions" %>  
      </div>
    </section>
    <section>
      <%= render "shared/change_stats" %>
    </section>
  </aside>
  
  <div class="span8">
    <h1 class="align-left">Employer Insurance Rates - History</h1>

    <div style="padding-bottom: 10px">
      <% if current_user.superuser? || current_user.administrator?(@country.country) %>
        <%= link_to "Add a new set of rates", "#", class: "btn" %>
        <%= link_to "Add a single rate", new_country_insurance_rate_path(@country), class: "btn" %>
      <% end %>
      <%= link_to "Back to main insurance menu", insurance_menu_country_path(@country), class: "btn" %>
    </div>
    <%= render "insurance_settings/warning" %>
    <div class="line-space">
      Listed below is the <u>full history</u> of National Insurance rates. <%= link_to "View current rates instead", 
        country_insurance_employer_ rates_path(@country), class: "btn" %>
    </div>
    <ul class="itemlist">
      <%= render "insurance_rates/list" %>
    </ul>
  </div>
</div>
