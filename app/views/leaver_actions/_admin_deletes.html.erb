            <% if current_user.superuser? %>
              | <%= link_to "del", leaveact, method: :delete,
                             data: { confirm: "Delete '#{leaveact.self_ref}'?  Sure?" } %>
            <% elsif current_user.admin? %>
              <% if leaveact.created_by == current_user.id %>
                | <%= link_to "del", leaveact, method: :delete,
                             data: { confirm: "Delete '#{leaveact.self_ref}'?  Sure?" } %>
              <% end %>
            <% end %>
