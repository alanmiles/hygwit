     <% if current_user.superuser? || current_user.administrator?(@country.country) %>
        <div class="standout line-space">
          When new threshold settings are announced, don't delete or edit the current values in case backdated adjustments
          are needed later.  Just add a new set of thresholds by clicking on 'Update all' above. 
        </div>
      <% end %>
