<% provide(:title, "Insurance: Current Salary Thresholds") %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @country.country %>
      </h1>
    </section>
    <section>
      <div class="instruction">
        <%= render "instructions" %>  
      </div>
    </section>
    <section>
      <%= render "shared/change_stats" %>
      <% if current_user.administrator?(@country.country) || current_user.superuser? %>
        <span id="change-note"><em>Changes are best seen in the history table</em></span>
      <% end %>
    </section>
  </aside>
  
  <div class="span8">
    <h1 class="align-left">Insurance: Current Salary Thresholds</h1>

    <div style="padding-bottom: 10px">
      <% if current_user.superuser? || current_user.administrator?(@country.country) %>
        <%= link_to "Add a line to the table", new_country_insurance_setting_path(@country), class: "btn" %>
      <% end %>
      <%= link_to "Back to main insurance menu", insurance_menu_country_path(@country), class: "btn" %>
    </div>
    <%= render "warning" %>
    <div class="line-space">
      Listed below are the <u>current</u> settings. 
      <%= link_to "View full history instead", country_insurance_history_settings_path(@country), class: "btn" %>
      <%= link_to "View future settings", 
         country_insurance_future_settings_path(@country), class: "btn" if @country.insurance_settings.future_settings? %>
    </div>
    <ul class="itemlist">
      <%= render "list" %>
    </ul>
  </div>
</div>
