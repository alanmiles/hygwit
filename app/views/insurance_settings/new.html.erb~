<% provide(:title, 'New Salary Threshold') %>
<div class="row">
  <aside class="span3">
    <section>
      <h1 class="line-space">
        <%= @country.country %>
      </h1>
      <div class="instruction">
        <div class="line-space">
          <%= insurance_settings_new_1 %>
        </div>
        <div class="line-space">
          <%= insurance_settings_new_2 %>
        </div> 
        <div class="line-space">
          <%= insurance_settings_new_3 %>
        </div> 
        <div>
          <%= insurance_settings_new_4 %>
        </div>  
      </div>
    </section>
  </aside>
  <div class="span6 offset0">
    <h1>New Salary Threshold</h1>
    <%= form_for([@country, @setting]) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <div class="line-space"><em>No blanks! All boxes below must be filled before your entry can be saved.</em></div>
      <%= render 'fields', f: f %>
      <%= f.submit "Create", class: "btn btn-primary" %>
      <%= link_to "Current thresholds list", country_insurance_settings_path(@country), class: "btn" %>
      <%= link_to "Main insurance menu", insurance_menu_country_path(@country), class: "btn" %>
    <% end %>
    <div class="top-line-space standout">
      After adding a new salary threshold, you need to update your list of National Insurance rates.  Create a new
      rate with the same effective date, to make sure it's included.
    </div>
  </div>
</div>
