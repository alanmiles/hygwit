            <% if current_user.superuser? %>
              | <%= link_to "del", payitem, method: :delete,
                             data: { confirm: "Delete '#{payitem.self_ref}'?  Sure?" } %>
            <% elsif current_user.admin? %>
              <% if payitem.created_by == current_user.id %>
                | <%= link_to "del", payitem, method: :delete,
                             data: { confirm: "Delete '#{payitem.self_ref}'?  Sure?" } %>
              <% end %>
            <% end %>
