            <% if current_user.superuser? %>
              | <%= link_to "del", joinact, method: :delete,
                             data: { confirm: "Delete '#{joinact.self_ref}'?  Sure?" } %>
            <% elsif current_user.admin? %>
              <% if joinact.created_by == current_user.id %>
                | <%= link_to "del", joinact, method: :delete,
                             data: { confirm: "Delete '#{joinact.self_ref}'?  Sure?" } %>
              <% end %>
            <% end %>
