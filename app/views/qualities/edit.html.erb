<% provide(:title, "Edit Quality") %> 
<h1>Edit Quality</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@quality) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= render 'fields', f: f %>
      
      <%= f.submit "Save change", class: "btn btn-primary" %>
      <%= link_to 'Back', @quality, class: "btn" if current_user.admin? %>
    <% end %>
  </div>
</div>

