<% provide(:title, 'Nationalities') %>

<div class="row">
  <aside class="span4">
    <section>
      <%= render 'shared/user_sidebar' %>   
    </section>
    <% if current_user.superuser? %>
      <div id="statistics">    
        <%= link_message %>
      </div>
  </aside>
  
  <div class="span8">
    <h1 class="align-left">Nationalities</h1>

    <div style="padding-bottom: 10px">
      <%= link_to "Add a nationality", new_nationality_path, class: "btn" %>
    </div>
    <ul class="itemlist">
      <% @nationalities.each do |nationality| %>
        <li>
          <%= nationality.nationality %>
          | <%= link_to "change", edit_nationality_path(nationality) %>
          <% unless nationality.linked? %>
            | <%= link_to "delete", nationality, method: :delete,
                             data: { confirm: "Delete '#{nationality.nationality}'?  Sure?" } %>
          <% end %>
        </li>
      <% end %>
    </ul>
    <% if Nationality.count > 10 %>
      <div style="padding-top: 10px">
        <%= link_to "Add a nationality", new_nationality_path, class: "btn" %>
      </div>
    <% end %>
  </div>
</div>
