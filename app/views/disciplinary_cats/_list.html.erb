      <% @disciplinary_cats.each do |category| %>
        <li>
          <%= category.category %>
          | <%= link_to "edit", edit_disciplinary_cat_path(category) %>
          | <%= link_to "del", disciplinary_cat_path(category), method: :delete,
                       data: { confirm: "Delete '#{category.category}'. Are you sure?" } %>
        </li>
      <% end %>
