<% provide(:title, "Edit New Salary Threshold") %> 
<div class="row">
  <aside class="span3">
    <section>
      <h1 class="line-space">
        <%= @country.country %>
      </h1>
      <div class="instruction">
        <div>
          <%= insurance_settings_country_guidance %>
        </div> 
      </div>
    </section>
  </aside>
  <div class="span6 offset0">
    <h1>Edit New Salary Threshold</h1>
    <%= form_for(@setting, url: insurance_threshold_path(@setting)) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= render 'edit_fields', f: f %>
      <%= f.submit "Save changes", class: "btn btn-primary" %>
      <%= link_to "Back to current salary thresholds", country_insurance_settings_path(@country), class: "btn" %>
      <%= link_to "Main insurance menu", insurance_menu_country_path(@country), class: "btn" %>
    <% end %>
  </div>
</div>
