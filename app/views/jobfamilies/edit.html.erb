<% provide(:title, "Edit Occupation") %> 
<h1>Edit Occupation</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@jobfamily) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= render 'fields', f: f %>
      
      <%= f.submit "Save change", class: "btn btn-primary" %>
      <%= link_to 'Back', jobfamilies_path, class: "btn" if current_user.admin? %>
    <% end %>
  </div>
</div>

