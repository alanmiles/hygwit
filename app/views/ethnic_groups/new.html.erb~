<% provide(:title, 'New Ethnic Group') %>
<div class="row">
  <aside class="span3">
    <section>
      <h1 class="line-space">
        <%= @country.country %>
      </h1>
      <div class="instruction">
        <div>
          render "instructions"
        </div> 
      </div>
    </section>
  </aside>
  <div class="span6 offset0">
    <h1>New Ethnic Group</h1>
    <%= form_for([@country, @egroup]) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create", class: "btn btn-primary" %>
      <%= link_to "All ethnic groups", country_ethnic_groups_path(@country), class: "btn" %>
      <%= link_to "Country set-up page", @country, class: "btn" %>
    <% end %>
  </div>
</div>
