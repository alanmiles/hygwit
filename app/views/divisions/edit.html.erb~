<% provide(:title, "Edit Division") %> 
<div class="row">
  <aside class="span3">
    <section>
      <h1 class="line-space">
        <%= @business.name %>
      </h1>
    </section>
    <section>
      <div class="instruction">
        <div class="line-space">
          <%= division_instruction_3 %>
        </div> 
        <% unless @division.has_current_departments? %>
          <div>
            <% if @division.current? %>
              <%= division_instruction_2 %>
            <% else %>
              <%= division_instruction_4 %>
            <% end %>
          </div>
        <% end %> 
      </div>
    </section>
    <% if @business.has_former_divisions? %>
      <section>
        <%= link_to "View / reinstate old divisions", business_old_divisions_path(@business), class: "btn" %>  
      </section>
    <% end %>
  </aside>
  <div class="span6 offset0">
    <h1>Edit Division</h1>
    <%= form_for(@division) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= render 'fields', f: f %>
      <%= f.submit "Save changes", class: "btn btn-primary" %>
      <%= link_to "All divisions", business_divisions_path(@business), class: "btn" %>
      <%= link_to "Business settings", @business, class: "btn" %>
    <% end %>
  </div>
</div>
