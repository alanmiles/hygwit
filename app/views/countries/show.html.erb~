<% provide(:title, @country.country) %>
<div class="row">
  <aside class="span3">
    <section>
      <h1>
        <%= @country.country %>
      </h1>
      <div><%= @country.nationality.nationality %></div>
      <div><%= @country.currency.currency_code %></div>
      
    </section>
    
      <%= link_to "All countries", countries_path, class: "btn" %>  
  </aside>
  <div class="span6 offset0 top-line-space content-block">
    <h3 class="line-space">Set-up parameters</h3>
    <div class="line-space">
      <%= link_to "Edit set-up parameters", edit_country_path(@country), class: "btn" %>
      <%= link_to "New coountry absences", new_country_country_absence_path(@country.id), class: "btn" %>
    </div>
    <%= render "setup_parameters" %>
  </div>
  <div class="span9 offset3 top-line-space content-block">  
    <h3 class="line-space">Absence codes</h3>
    <%= render "absence_codes" %>
  </div> 
</div>

