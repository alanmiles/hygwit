module PayCategoriesHelper

  def paycat_instruction_1
    "Payroll items are grouped into categories for the purposes of analysis.  For example, housing and transport allowances are
     grouped as 'Fixed Benefits'."
  end
  
  def paycat_instruction_2
    "While the actual payroll items are likely to vary from country to country and from business to business, the categories we
     set here will apply to all businesses everywhere."
  end
  
  def paycat_instruction_3
    "The category list will always be ordered as displayed here.  Drag and drop using the red arrows in the '(move)' column 
     if the order needs to be changed."
  end
end
